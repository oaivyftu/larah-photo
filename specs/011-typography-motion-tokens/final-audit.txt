DESIGN SYSTEM AUDIT
------------------------------------------------------------------------
Scanned 24 component stylesheets.

Inspects: colour, type-size, line-height, font-weight, letter-spacing, spacing, duration, easing, local-token.
Does NOT inspect, on purpose:
  - border-radius
  - z-index
  - width / height / min-* / max-*
  - outline-offset
  - opacity
  - motion held in interaction code
Reasons are in COVERAGE at the top of this script. A passing run
claims nothing about the groups above.
Every value below must resolve through src/styles/. Use count sets
the order to fix them in, not whether they must be fixed.
A local-token entry is a component-local custom property holding a
raw value: a private token, the anti-pattern Principle VII names.

(b) USED 2+ TIMES -- already drifting; fix first
------------------------------------------------------------------------
  none

(c) USED ONCE -- not drifting yet, and equally not owned
------------------------------------------------------------------------
  none

(a) TOTALS
------------------------------------------------------------------------
  colour      0 distinct values to tokenise ( 0 repeated across   0 uses,   0 used once)
  type-size   0 distinct values to tokenise ( 0 repeated across   0 uses,   0 used once)
  line-height  0 distinct values to tokenise ( 0 repeated across   0 uses,   0 used once)
  font-weight  0 distinct values to tokenise ( 0 repeated across   0 uses,   0 used once)
  letter-spacing  0 distinct values to tokenise ( 0 repeated across   0 uses,   0 used once)
  spacing     0 distinct values to tokenise ( 0 repeated across   0 uses,   0 used once)
  duration    0 distinct values to tokenise ( 0 repeated across   0 uses,   0 used once)
  easing      0 distinct values to tokenise ( 0 repeated across   0 uses,   0 used once)
  local-token  0 distinct values to tokenise ( 0 repeated across   0 uses,   0 used once)

------------------------------------------------------------------------
PASS
