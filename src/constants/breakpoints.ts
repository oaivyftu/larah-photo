// Mirrors src/styles/_breakpoints.scss — keep these two files in sync manually,
// since SCSS variables aren't importable into JS without extra build tooling.
export const BREAKPOINT_PHONE_LG = 620;
export const BREAKPOINT_TABLET_LG = 900;
